#https://taskfile.dev/
version: '3'

tasks:

  tidy:
    desc: 'run Go mod tidy'
    cmds:
      - go mod tidy

  format:
    desc: 'formats the code'
    deps:
      - task: check-go-tools
        vars:
          TOOLS: [GOFUMPT, BETTERALIGN]
    cmds:
      - go fix ./...
      - $(check-go-tool {{.BETTERALIGN}}) -apply ./... || true
      - $(check-go-tool {{.GOFUMPT}}) -extra -l -w .

  lint:
    desc: 'run Go linters'
    deps:
      - task: check-go-tools
        vars:
          TOOLS: [STATICCHECK, BETTERALIGN, REVIVE]
    cmds:
      - go vet ./...
      - $(check-go-tool {{.REVIVE}}) -config revive.toml -formatter friendly -set_exit_status ./...
      - $(check-go-tool {{.STATICCHECK}}) ./...
      - $(check-go-tool {{.BETTERALIGN}}) ./...

  licenses:
    desc: 'check licenses'
    deps:
      - task: check-go-tool
        vars:
          TOOL: GO_LICENSES
    cmds:
      - |
          PROJECT="$(go list -m)"
          ALLOWED_LICENSES="$(<.allowed_license_types)"
          $(check-go-tool {{.GO_LICENSES}}) report --ignore "$PROJECT" --ignore "github.com/golang/freetype/raster" --ignore "github.com/golang/freetype/truetype" --ignore "oss.terrastruct.com/d2" --ignore "github.com/cyphar/filepath-securejoin" .
          $(check-go-tool {{.GO_LICENSES}}) check --allowed_licenses="$ALLOWED_LICENSES" --ignore "$PROJECT"  --ignore "github.com/oktalz/present" --ignore "github.com/golang/freetype/raster" --ignore "github.com/golang/freetype/truetype" --ignore "oss.terrastruct.com/d2"  --ignore "github.com/cyphar/filepath-securejoin" .

  govulncheck:
    desc: 'check vulnerabilities in Go code'
    deps:
      - task: check-go-tool
        vars:
          TOOL: GOVULNCHECK
    cmds:
      - $(check-go-tool {{.GOVULNCHECK}}) -show verbose ./...
